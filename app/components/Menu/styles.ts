import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    menuContainer: {
        position: "absolute",
        top: 35,
        right: 35,
        backgroundColor: "#fff",
        borderRadius: 10,
    },
    menuButton: {
        paddingVertical: 17,
        paddingHorizontal: 20,
    },
});