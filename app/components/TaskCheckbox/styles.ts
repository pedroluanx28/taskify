import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    checkbox: {
        height: 24,
        width: 24,
        borderRadius: 12,
        borderWidth: 2,
        borderColor: "#B29AEC",
        justifyContent: "center",
        alignItems: "center",
    },
    checked: {
        backgroundColor: "#B29AEC",
    }
});